worker_processes auto;
error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events { worker_connections 1024; }

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile on;
    keepalive_timeout 65;
    client_max_body_size 500m;

    upstream django { server backend:8000; }

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    # ------------- HTTP : редирект на HTTPS + acme -------------
    server {
        listen 80;
        server_name mycloud-diploma.duckdns.org;

        root /usr/share/nginx/html;
        index index.html;

        # health для docker healthcheck (без редиректа)
        location = /health {
            add_header Content-Type text/plain;
            return 200 "ok";
        }

        # http-01 challenge
        location ^~ /.well-known/acme-challenge/ {
            default_type "text/plain";
            try_files $uri =404;
        }

        # Точный редирект /admin -> https://.../admin/
        location = /admin {
            return 301 https://$host/admin/;
        }

        # Остальное — общий редирект на HTTPS
        location / {
            return 301 https://$host$request_uri;
        }
    }

    # -------------------- HTTPS основной -----------------------
    server {
        listen 443 ssl;
        http2 on;
        server_name mycloud-diploma.duckdns.org;

        ssl_certificate     /etc/letsencrypt/live/mycloud-diploma.duckdns.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/mycloud-diploma.duckdns.org/privkey.pem;
        ssl_session_timeout 1d;
        ssl_session_cache shared:SSL:10m;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;

        # Корень фронта
        root /usr/share/nginx/html;
        index index.html;

        # Безопасные заголовки (базовые)
        add_header X-Content-Type-Options nosniff;
        add_header X-Frame-Options SAMEORIGIN;
        add_header Referrer-Policy strict-origin-when-cross-origin;

        # health (дополнительно и на HTTPS)
        location = /health {
            add_header Content-Type text/plain;
            return 200 "ok";
        }

        # Django Admin
        location ^~ /dj_admin/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # --- API ---
        location ^~ /api/ {
            proxy_pass http://django;
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade           $http_upgrade;
            proxy_set_header Connection        $connection_upgrade;
        }

        # Redirect /admin  →  /admin/
        location = /admin {
            return 301 /admin/;
        }

        # --- Публичные ссылки /d/<token> ---
        location ^~ /d/ {
            proxy_pass http://django;
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # --- Статика Vite (хэшир. бандлы) ---
        location ^~ /assets/ {
            try_files $uri =404;
            access_log off;
            expires 30d;
            add_header Cache-Control "public, max-age=2592000, immutable";
        }

        # --- Django STATIC (/static/) ---
        location ^~ /static/ {
            try_files $uri =404;
            access_log off;
            expires 30d;
            add_header Cache-Control "public, max-age=2592000, immutable";
        }

        # Явно отдаём index.html как файл (без циклов)
        location = /index.html {
            try_files $uri =404;
        }

        # --- SPA fallback: сначала файл/каталог, иначе index.html ---
        location / {
            try_files $uri $uri/ /index.html;
        }

        # favicon — необязателен
        location = /favicon.ico {
            try_files /favicon.ico =204;
            access_log off;
        }

        error_page 500 502 503 504 /50x.html;
        location = /50x.html { root /usr/share/nginx/html; }
    }
}
